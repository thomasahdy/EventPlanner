server {
    listen 8080;
    server_name _;

    root /opt/app-root/src;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    error_page 403 404 /index.html;
}
